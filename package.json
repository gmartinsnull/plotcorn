{
  "private": true,
  "name": "plotcorn",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "yarn --cwd apps/web start",
    "next": "yarn --cwd apps/web-app dev",
    "dev": "npm-run-all --parallel start next"
  }
}
